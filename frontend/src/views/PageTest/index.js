import React from 'react';
import PropTypes from 'prop-types';

//import './style.scss';

const pageTest = props => {
  return (
   <div>
     pageTest
   </div>
  );
};

pageTest.propTypes = {
  
};

export default pageTest;
